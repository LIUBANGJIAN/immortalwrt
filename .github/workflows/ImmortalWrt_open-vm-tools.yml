name: Build_ImmortalWrt_x86_Combined_Only

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/LIUBANGJIAN/immortalwrt
  REPO_BRANCH: master
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Extreme Space Cleanup (Ref LEDE)
      run: |
        # 参照 LEDE 脚本彻底删除大文件，特别是 .ghcup 和 dotnet
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup /usr/lib/jvm /etc/mysql /etc/php /var/lib/mongodb /usr/share/kotlinc /usr/local/share/edge_driver /usr/local/share/gecko_driver /opt/microsoft/powershell /opt/pipx /usr/share/azure-cli /opt/az
        
        sudo -E apt-get update
        sudo -E apt-get -y install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool llvm lrzsz ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        df -h

    - name: Clone and Update
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        cd openwrt
        echo "src-git lucky https://github.com/gdy666/luci-app-lucky.git" >> ./feeds.conf.default
        echo "src-git small https://github.com/kenzok8/small.git" >> ./feeds.conf.default
        echo "src-git openlist2 https://github.com/sbwml/luci-app-openlist2.git" >> ./feeds.conf.default
        ./scripts/feeds update -a
        ./scripts/feeds install -a -f

    - name: Generate Config
      run: |
        cd openwrt
        # 1. 架构配置
        echo "CONFIG_TARGET_x86=y" > .config
        echo "CONFIG_TARGET_x86_64=y" >> .config
        echo "CONFIG_TARGET_x86_64_DEVICE_generic=y" >> .config
        
        # 2. 插件配置
        echo "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Trojan_Plus=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-lucky=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-openlist2=y" >> .config
        echo "CONFIG_PACKAGE_open-vm-tools=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-ttyd=y" >> .config
        
        # 3. 核心空间优化：只生成 combined-efi.img，禁用其他格式
        echo "CONFIG_TARGET_IMAGES_GZ=y" >> .config
        echo "CONFIG_TARGET_ROOTFS_TARGZ=n" >> .config
        echo "CONFIG_TARGET_IMAGES_PAD=n" >> .config
        echo "CONFIG_VMDK_IMAGES=n" >> .config
        echo "CONFIG_VDI_IMAGES=n" >> .config
        echo "CONFIG_VHDX_IMAGES=n" >> .config
        echo "CONFIG_QCOW2_IMAGES=n" >> .config
        
        # 禁用调试符号以节省数GB空间
        echo "CONFIG_DEBUG_INFO=n" >> .config
        
        make defconfig
        make download -j8
        find dl -size -1024c -exec rm -f {} \;

    - name: Build Firmware
      id: compile
      run: |
        cd openwrt
        # 使用 2 线程编译，平衡速度与磁盘压力
        make -j2 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Organize Artifacts
      if: steps.compile.outputs.status == 'success'
      run: |
        cd openwrt/bin/targets/x86/64/
        # 再次确认清理：只保留 combined-efi 结尾的文件
        rm -rf packages
        ls -a
        mkdir -p /tmp/upload
        # 移动 efi 固件到临时目录，并改一个好识别的名字
        cp *combined-efi.img.gz /tmp/upload/immortalwrt-x86-64-combined-efi.img.gz || true
        echo "FIRMWARE=/tmp/upload" >> $GITHUB_ENV

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      if: steps.compile.outputs.status == 'success'
      with:
        name: ImmortalWrt_x86_64_EFI
        path: ${{ env.FIRMWARE }}/*

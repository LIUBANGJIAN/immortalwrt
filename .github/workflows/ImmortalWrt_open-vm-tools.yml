name: Build_ImmortalWrt_Latest_Stable_Full_CN

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Extreme Space Cleanup
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup /usr/lib/jvm /etc/mysql /etc/php /var/lib/mongodb /usr/share/kotlinc /usr/local/share/edge_driver /usr/local/share/gecko_driver /opt/microsoft/powershell /opt/pipx /usr/share/azure-cli /opt/az /usr/lib/google-cloud-sdk
        sudo -E apt-get update
        sudo -E apt-get -y install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool llvm lrzsz ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean

    - name: Clone Source & Auto Switch to Latest Stable
      run: |
        # 自动获取最新的正式发行版 Tag (例如 v23.05.3)
        LATEST_TAG=$(curl -s https://api.github.com/repos/immortalwrt/immortalwrt/releases/latest | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/')
        echo "Detected Latest Stable Tag: $LATEST_TAG"
        # 转换为分支名 (例如 openwrt-23.05)
        STABLE_BRANCH="openwrt-${LATEST_TAG%.*}"
        echo "Switching to Branch: $STABLE_BRANCH"
        git clone $REPO_URL -b $STABLE_BRANCH openwrt
        echo "STABLE_VERSION=$LATEST_TAG" >> $GITHUB_ENV

    - name: Update Feeds & Add Custom Repos
      run: |
        cd openwrt
        echo "src-git lucky https://github.com/gdy666/luci-app-lucky.git" >> ./feeds.conf.default
        echo "src-git small https://github.com/kenzok8/small.git" >> ./feeds.conf.default
        echo "src-git openlist2 https://github.com/sbwml/luci-app-openlist2.git" >> ./feeds.conf.default
        echo "src-git netbird https://github.com/tbc0309/openwrt-netbird.git" >> ./feeds.conf.default
        echo "src-git rtp2httpd https://github.com/stackia/rtp2httpd.git" >> ./feeds.conf.default
        echo "src-git taskplan https://github.com/sirpdboy/luci-app-taskplan.git" >> ./feeds.conf.default
        ./scripts/feeds update -a
        ./scripts/feeds install -a -f

    - name: Custom System Settings (Timezone & Web)
      run: |
        cd openwrt
        # 1. 设置默认时区为上海
        sed -i 's/UTC/CST-8/g' package/base-files/files/bin/config_generate
        sed -i '/timezone/d' package/base-files/files/bin/config_generate
        sed -i '/set system.@system\[0\].hostname/a \ \ \ \ \ \ \ \ set system.@system[0].timezone='\''CST-8'\''' package/base-files/files/bin/config_generate
        sed -i '/set system.@system\[0\].timezone/a \ \ \ \ \ \ \ \ set system.@system[0].zonename='\''Asia/Shanghai'\''' package/base-files/files/bin/config_generate
        
        # 2. 修正 Web 自动跳转 (尝试增加 uhttpd 强制)
        sed -i 's/option redirect_https .*/option redirect_https 0/g' package/network/services/uhttpd/files/uhttpd.config || true

    - name: Generate Config
      run: |
        cd openwrt
        echo "CONFIG_TARGET_x86=y" > .config
        echo "CONFIG_TARGET_x86_64=y" >> .config
        echo "CONFIG_TARGET_x86_64_DEVICE_generic=y" >> .config
        
        # --- 核心：LuCI & 中文 ---
        echo "CONFIG_PACKAGE_luci=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
        echo "CONFIG_PACKAGE_luci-theme-argon=y" >> .config
        
        # --- 你的新插件需求 ---
        echo "CONFIG_PACKAGE_luci-app-filetransfer=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-filetransfer-zh-cn=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-nlbwmon=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-nlbwmon-zh-cn=y" >> .config
        
        # --- 之前的所有插件 ---
        echo "CONFIG_PACKAGE_luci-app-passwall=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-lucky=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-openlist2=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-ttyd=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-wireguard=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-frps=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-netdata=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-taskplan=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-rtp2httpd=y" >> .config
        echo "CONFIG_PACKAGE_netbird=y" >> .config
        echo "CONFIG_PACKAGE_iperf3=y" >> .config
        echo "CONFIG_PACKAGE_iftop=y" >> .config
        
        # 中文包补全 (确保所有app都有中文)
        echo "CONFIG_LUCI_LANG_zh_Hans=y" >> .config

        # 分区与镜像
        echo "CONFIG_TARGET_IMAGES_GZ=y" >> .config
        echo "CONFIG_TARGET_ROOTFS_PARTSIZE=400" >> .config
        
        make defconfig
        make download -j8

    - name: Build Firmware
      id: compile
      run: |
        cd openwrt
        make tools/install -j$(nproc) || make tools/install -j1 V=s
        make toolchain/install -j$(nproc) || make toolchain/install -j1 V=s
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        echo "release_tag=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT

    - name: Organize Artifacts
      if: steps.compile.outputs.status == 'success'
      run: |
        cd openwrt/bin/targets/x86/64/
        mkdir -p /tmp/artifact/
        cp *squashfs-combined-efi.img.gz /tmp/artifact/immortalwrt-stable-efi.img.gz || true
        cp *squashfs-combined.img.gz /tmp/artifact/immortalwrt-stable-legacy.img.gz || true
        echo "FIRMWARE_PATH=/tmp/artifact" >> $GITHUB_ENV

    - name: Upload to Release
      uses: svenstaro/upload-release-action@v2
      if: steps.compile.outputs.status == 'success'
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: ${{ env.FIRMWARE_PATH }}/*
        tag: Latest-Release
        release_name: "ImmortalWrt Stable [${{ env.STABLE_VERSION }}]"
        overwrite: true
        file_glob: true

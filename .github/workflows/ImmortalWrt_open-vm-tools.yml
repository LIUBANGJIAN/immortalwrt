name: Build_ImmortalWrt_Final_Fix_V6

on:
  workflow_dispatch:
    inputs:
      branch:
        description: '选择编译分支'
        required: true
        default: 'openwrt-24.10'

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  TARGET_BRANCH: ${{ github.event.inputs.branch }}
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Space Cleanup & Environment Setup
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /usr/share/swift /usr/local/.ghcup /usr/lib/jvm /etc/mysql /etc/php /var/lib/mongodb /usr/share/kotlinc /usr/local/share/edge_driver /usr/local/share/gecko_driver /opt/microsoft/powershell /opt/pipx /usr/share/azure-cli /opt/az /usr/lib/google-cloud-sdk
        sudo -E apt-get update
        sudo -E apt-get -y install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool llvm lrzsz ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        # 安装最新的 Go 环境，防止 Netbird 编译失败
        sudo snap install go --classic
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean

    - name: Clone Source
      run: git clone $REPO_URL -b ${{ env.TARGET_BRANCH }} openwrt

    - name: Update Feeds & Intelligent Override
      run: |
        cd openwrt
        echo "src-git lucky https://github.com/gdy666/luci-app-lucky.git" >> ./feeds.conf.default
        echo "src-git small https://github.com/kenzok8/small.git" >> ./feeds.conf.default
        echo "src-git netbird_custom https://github.com/tbc0309/openwrt-netbird.git" >> ./feeds.conf.default
        echo "src-git rtp2httpd https://github.com/stackia/rtp2httpd.git" >> ./feeds.conf.default
        echo "src-git taskplan https://github.com/sirpdboy/luci-app-taskplan.git" >> ./feeds.conf.default
        
        ./scripts/feeds update -a
        
        # 智能覆盖：删除官方仓库冲突项
        echo "清理冲突插件..."
        for pkg in netbird luci-app-netbird rtp2httpd luci-app-rtp2httpd; do
          find feeds/packages feeds/luci -name "$pkg" -type d -exec rm -rf {} + || true
        done
        
        # 单独拎出最新的 open-vm-tools
        git clone https://github.com/vmware/open-vm-tools.git package/open-vm-tools-latest

        ./scripts/feeds install -a -f

    - name: Generate Config
      run: |
        cd openwrt
        echo "CONFIG_TARGET_x86=y" > .config
        echo "CONFIG_TARGET_x86_64=y" >> .config
        echo "CONFIG_TARGET_x86_64_DEVICE_generic=y" >> .config
        
        # 基础组件
        echo "CONFIG_PACKAGE_luci=y" >> .config
        echo "CONFIG_PACKAGE_luci-compat=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
        
        # 驱动 (集成你要求的初始化包)
        for drv in autocore automount block-mount fdisk fstools kmod-fs-f2fs kmod-8139cp kmod-e1000e kmod-igc kmod-r8125 kmod-vmxnet3 open-vm-tools kmod-ata-piix kmod-scsi-core kmod-mptfusion; do
          echo "CONFIG_PACKAGE_$drv=y" >> .config
        done

        # 插件 (确保名称正确)
        for app in lucky netbird luci-app-netbird rtp2httpd luci-app-rtp2httpd luci-app-taskplan luci-app-wireguard luci-proto-wireguard iperf3 tcpdump pv luci-app-passwall luci-app-ttyd; do
          echo "CONFIG_PACKAGE_$app=y" >> .config
        done
        
        echo "CONFIG_TARGET_ROOTFS_PARTSIZE=600" >> .config
        make defconfig
        make download -j8

    - name: Build Firmware
      id: compile
      run: |
        cd openwrt
        # 使用 -j$(nproc) 提速，如果失败则转为单线程排查
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Organize & Rename
      if: steps.compile.outputs.status == 'success'
      run: |
        cd openwrt
        V_TAG=$(echo ${{ env.TARGET_BRANCH }} | sed 's/openwrt-//')
        
        # 统计插件用于 Release Body
        echo "PLUGINS_LIST<<EOF" >> $GITHUB_ENV
        grep "CONFIG_PACKAGE_luci-app-" .config | grep "=y" | cut -d'_' -f3- | cut -d'=' -f1 | sed 's/^/- /' >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV
        
        F_PATH="bin/targets/x86/64"
        mkdir -p /tmp/upload/
        find $F_PATH -name "*combined-efi.img.gz" -exec cp {} /tmp/upload/immortalwrt-x86_64-${V_TAG}-efi.img.gz \;
        find $F_PATH -name "*combined.img.gz" ! -name "*efi*" -exec cp {} /tmp/upload/immortalwrt-x86_64-${V_TAG}-legacy.img.gz \;

    - name: Upload to Release
      if: steps.compile.outputs.status == 'success'
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: /tmp/upload/*
        tag: Latest-Release
        release_name: "ImmortalWrt-${{ env.TARGET_BRANCH }}-x86_64"
        body: |
          ### 插件清单:
          ${{ env.PLUGINS_LIST }}
        overwrite: true
        file_glob: true

    - name: Delete Old Releases & Tags
      uses: dev-drprasad/delete-older-releases@v0.3.3
      with:
        keep_latest: 0
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
